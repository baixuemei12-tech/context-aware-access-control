spring.application.name=demo
server.port=5050

# Fabric Network Configuration
fabric.channelName = mychannel
fabric.chaincodeName = caac
fabric.mspId = Org1MSP

fabric.cryptoPath = ${CAAC_FABRIC_CRYPTO_PATH}

# Endpoint of the Org1 Peer
fabric.peerEndpoint = localhost:7051
fabric.peerOverrideAuthority = peer0.org1.example.com
oracle.hmac.key=${CAAC_ORACLE_HMAC_KEY}
